※Master側
CREATE DATABASE enc_test;
USE enc_test;
CREATE TABLE enc_test (id int(10) PRIMARY KEY AUTO_INCREMENT, value VARCHAR(100)) ENGINE=innodb ENCRYPTION='Y';
INSERT INTO enc_test SET value='hoge';
INSERT INTO enc_test SET value='fuga';
---
SHOW BINARY LOGS;
CREATE USER 'repl'@'%' IDENTIFIED BY 'T35+U53r';
GRANT REPLICATION SLAVE ON *.* TO 'repl'@'%';
---
※Slave側
CHANGE MASTER TO
    MASTER_HOST='【ホスト名】',
    MASTER_USER='repl',
    MASTER_PASSWORD='T35+U53r',
    MASTER_LOG_FILE='binlog.000001',
    MASTER_LOG_POS=4;
START SLAVE;
SHOW SLAVE STATUS\G
SET GLOBAL SQL_SLAVE_SKIP_COUNTER=1;
START SLAVE;
SHOW SLAVE STATUS\G